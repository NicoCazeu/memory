<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="ThemeOverlay.AppCompat.Dark">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blackColor</item>
        <item name="colorPrimaryDark">@color/blackColor</item>
        <item name="colorAccent">@color/blackColor</item>
    </style>

    <style name="LoadingProgressBar" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="colorAccent">@color/yellowColor</item>
    </style>
</resources>
